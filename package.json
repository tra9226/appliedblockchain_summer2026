{
  "name": "credchain",
  "version": "1.0.0",
  "description": "Hash-anchored academic credential registry (diploma/transcript verification) on Ethereum.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "hash": "node offchain/hashRecord.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
